# Recommender System (ALS)
# Implement the ALS-based recommender system here

#from pyspark.ml.recommendation import ALS

#def train_als_model(df):
   # als = ALS(maxIter=10, regParam=0.01, userCol="user", itemCol="item", ratingCol="rating")
   # model = als.fit(df)
    #return model